export interface ProductType {
  _id: string;
  name: string;
  price: number;
  seed: string;
  seedType: string;
  image: string;
  label: string;
  description: string;
  caffeine: string;
  weight: number;
  createdAt: string;
  updatedAt: string;
}
